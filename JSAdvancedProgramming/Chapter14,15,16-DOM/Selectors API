//querySelector()  返回匹配的第一个后代元素
//querySelectorAll()  返回匹配的所有元素

// childrenElementCount 返回Element类型的子元素数量（不包含文本节点和注释）
// firstElementChild 返回第一个
// lastElementChild 返回最后一个
// previousElementSibling 指向前一个Element类型的同胞元素
// nextElementSibling 指向后一个Element类型的同胞元素
let firstElement = document.firstElementChild

// getElementByClassName() 传入一个或付哦哥类名的字符串

//classList 属性 可以操作calss
// add()  添加类
// contains() 返回boolean 判断存在
// remove() 删除类
// aoggle()  有删除 没有添加

//自定义数据属性 data-  
//元素可以通过 dataset属性访问 包含一组键值对映射
// <div data-id="1"></div> 或 div.dataset.id = 1
console.log(div.dataset.id) //1

//插入标记 
// innerHTML 属性 替换元素 包括标签
// outerHTML 属性 读取元素 包括标签
//innerText 替换文本 不包括标签
//outerText 属性 读取本文 不包括标签
div.innerHTML = "hello world"

//  children 属性  只包含元素类型的节点

