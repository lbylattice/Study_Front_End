1.登陆https://github.com/
2.创建新仓库 按钮(new repository)
3.输入repository name 和 description 然后点击 create repository
4.先复制你的远程仓库，然后在本地将远程仓库的代码clone下来
例： git clone https://github.com/lbylattice/Study_Front_End
5.push 代码


或
1.git init 初始化操作，但是你的项目里的文件还没有被跟踪
2.通过 git add 命令来指定所需的文件来进行追踪，然后执行 git commit， 得到了一个存在被追踪文件与初始提交的 Git 仓库。
例：
git add *.c
git add LICENSE
git commit -m 'initial project version'


或
Git 克隆的是该 Git 仓库服务器上的几乎所有数据，而不是仅仅复制完成你的工作所需要文件。 
当你执行 git clone 命令的时候，默认配置下远程 Git 仓库中的每一个文件的每一个版本都将被拉取下来。
事实上，如果你的服务器的磁盘坏掉了，你通常可以使用任何一个克隆下来的用户端来重建服务器上的仓库。
例：git clone https://github.com/libgit2/libgit2
在当前目录下创建一个名为 “libgit2” 的目录，并在这个目录下初始化一个 .git 文件夹

