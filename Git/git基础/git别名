不想每次都输入完整的 Git 命令，可以通过 git config 文件来轻松地为每一个命令设置一个别名。命令(git config --global alias.xxx xxxxx)
例:
git config --global alias.unstage 'reset HEAD --'
这会使下面的两个命令等价：
git unstage fileA
git reset HEAD -- fileA